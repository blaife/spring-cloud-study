server:
  port: 3345
spring:
  application:
    name: config-client
  cloud:
    config:
#      label: master
      name: configtest
#      profile: dev
      uri: http://localhost:3344

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
